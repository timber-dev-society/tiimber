<?php
namespace Tiimber\Consts\Http;

const PORT = 'port';

const HOST = 'host';

const CODE = 'code';

const HEADER = 'headers';

const REQUEST = 'request';

const DEFAULT_HEADERS = ['Content-Type' => 'text/html; charset=utf-8'];